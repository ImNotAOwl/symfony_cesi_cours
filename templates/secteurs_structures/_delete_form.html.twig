<form method="post" action="{{ path('app_secteurs_structures_delete', {'id': secteurs_structure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secteurs_structure.id) }}">
    <button class="btn">Delete</button>
</form>
